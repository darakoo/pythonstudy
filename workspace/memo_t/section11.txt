[ch05 파이썬의 함수] - [section11 사용자 함수] 

### 목차 ###
01. 사용자 함수
    1. 사용자 함수란?
    - 사용자가 직접 만드는  함수

    2. 생활 속의 함수
02. 사용자 함수 용어
    1. 함수 용어 정리
    - 인수, 매개변수, 반환값
    2. 함수 정의
    def 함수이름(매개변수):
        기능구현
        return 반환값

    3. 함수 호출
    함수이름(인수)
    변수 = 함수이름(인수)

03. 인수(argument)와 매개변수(parameter)
    1. 인수가 있는 함수
    2. 가변 매개변수
    3. 디폴트 매개변수(기본 매개변수)
04. 반환값
    1. 반환값이 있는 함수
    2. 다중 반환
    3. 함수의 종료를 위한 return
05. 지역변수와 전역변수
    1. 지역변수
    2. 전역변수


### MISSION ###
- def 키워드를 통해서 새로운 함수를 정의할 수 있다.
- 함수에 전달하는 값을 인수라고 한다.
- 인수를 저장하는 변수를 매개변수라고 한다.
- 함수의 변환값이 존재하는 경우 return문을 통해 반환할 수 있다.
- 반환값이 없는 함수에서 return문을 사용하면 함수를 종료할 수 있다.

### 상세 ###